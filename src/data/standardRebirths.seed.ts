/**
 * Backwards-compatible alias for the rebirth cycle data. The full table
 * (4 cycles × 23 levels) lives in `rebirthCycles.seed.ts`. Selectors
 * filter by active cycle.
 */
export { REBIRTH_CYCLES as STANDARD_REBIRTHS } from "./rebirthCycles.seed";
